(currently pretty messy...) workspace for research on rare event simulation with variational quantum algorithms + tuned with classical Adam optimization. 
modeled after generic structural reliability problem with function of sum of squares, load variables x_i ~ Exp(1).
